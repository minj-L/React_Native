import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    buttons_container : {
        flexDirection : 'row',
    },
    buttons_view : {
        width : 100,
        height : 100,
        borderRadius : 50,
        backgroundColor : 'black',
    }
})