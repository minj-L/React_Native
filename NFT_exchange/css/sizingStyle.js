import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    sizing_container : {
        flex : 1,
        flexDirection : 'row',
    },
    sizing_view : {
        width : 100,
        height : 100,
        borderRadius : 50,
        backgroundColor : 'black',
    }
})